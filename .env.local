# NE JAMAIS COMMITER CE FICHIER. Ajoute-le à .gitignore : echo ".env.local" >> .gitignore
#
# Usage local (exemple avec direnv, ou juste `export $(cat .env.local | xargs)` avant de lancer
# ./gradlew bootRun) :

export DB_URL="jdbc:postgresql://ep-divine-union-atuvsnzt-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require"
export DB_USERNAME="neondb_owner"
export DB_PASSWORD="npg_2ZL0ystJkzPr"

# A définir aussi, sinon Spring ne démarrera pas (valeurs de test possibles en attendant le vrai bucket/queue) :
export AWS_S3_BUCKET="ton-bucket-s3"
export AWS_SQS_IMAGE_PROCESSING_QUEUE_URL="https://sqs.eu-west-3.amazonaws.com/xxxx/ta-queue"